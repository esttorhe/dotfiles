# ABOUTME: Tool abbreviations for fish shell
# ABOUTME: Sourced automatically by fish from conf.d/

# Editor
abbr -a vim nvim
abbr -a vi nvim

# Modern replacements
abbr -a la 'eza -abghHliS --git --icons=always'
abbr -a cat bat
abbr -a lg lazygit
abbr -a ld lazydocker

# Development
abbr -a be 'bundle exec'
abbr -a gotr 'go test -run'

# Misc
abbr -a rmorig 'rm -rf **/**.orig'
abbr -a claudio 'claude --dangerously-skip-permissions'
